package com.ktb;

// TODO: accept stream of input events and spout them off
public class PlaybackInputHandler implements InputHandler {
    @Override
    public void subscribe(InputEvent event, Subscriber<? extends InputEvent, ? extends InputEventData> subscriber) {

    }

    @Override
    public void unsubscribe(InputEvent event, Subscriber<? extends InputEvent, ? extends InputEventData> subscriber) {

    }

    @Override
    public void dispatch(InputEvent event, InputEventData data) {

    }
}
